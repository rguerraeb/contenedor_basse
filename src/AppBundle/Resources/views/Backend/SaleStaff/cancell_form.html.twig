<div class="box box-primary collapsed-box">
    <div class="box-header with-border">
        <h3 class="box-title">
            <i class='fa fa-remove'></i>
            Cancelar venta
        </h3>

        <div class="box-tools pull-right">
            <button type="button" class="btn btn-box-tool" data-widget="collapse">
                <i class="fa fa-plus"></i>
            </button>
        </div>
    </div>

    {{ form_start(
        form,
        {
            'action': path('backend_sale_cancell'),
            'method': 'POST',
            'attr': {'id': 'cancel-form'}
        }
        ) }}
        <div class="box-body">
            <div class="row">
                <div class="col-md-12">
                    <div class="form-group">
                        {{ form_label(form.smsString, "Número de motor") }}
                        {{form_widget(form.smsString, {'attr': {'class': 'required form-control' }}) }}
                    </div>
                </div>
            </div>
        </div>
        <div class="box-footer">
            <button type="submit" class="btn btn-primary pull-right" id="cancel-sale-staff">
                Cancelar venta
            </button>
        </div>
    {{ form_end(form) }}
</div>